[{% for post in site.posts %}
{"title" : "{{ post.title }}", "url" : "{{ post.url }}","date" : "{{ post.date | date_to_string }}"}
{% if forloop.rindex0 != 0 %},{% endif %}
{% endfor %}
];

{%comment%}
"content" : "{{ post.content |  strip_html | truncate : 120 }}"}
{%endcomment%}